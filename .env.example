# Tunnel Base URL (Router Tunnel)
TUNNEL_BASE_URL=https://<your_subdomain>.lhr.life

# Server-only authenticated proxy token secret (at least 32 random characters)
PROXY_SECRET=replace_with_a_long_random_server_only_secret
# Optional only while rotating old AES-CBC references; remove after stored refs expire/migrate
PROXY_SECRET_LEGACY=

# Client-side AES encryption key (public by design; do not reuse for sessions)
NEXT_PUBLIC_CRYPTO_SECRET=replace_with_client_payload_key

# PostgreSQL
DATABASE_URL=postgresql://user:password@localhost:5432/alex_cinema?schema=public

# Public application origin used by OAuth redirects
APP_ORIGIN=https://cinax.live

# Clerk
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_live_replace_me
CLERK_SECRET_KEY=sk_live_replace_me
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_SIGN_IN_FALLBACK_REDIRECT_URL=/home
NEXT_PUBLIC_CLERK_SIGN_UP_FALLBACK_REDIRECT_URL=/home

# Telegram Mini App and OpenID Connect
TELEGRAM_BOT_TOKEN=replace_with_bot_token
TELEGRAM_CLIENT_ID=replace_with_oidc_client_id
TELEGRAM_CLIENT_SECRET=replace_with_oidc_client_secret

# Server-only signing secret (at least 32 random characters)
TELEGRAM_SESSION_SECRET=
NEXT_PUBLIC_TELEGRAM_BOT_URL=https://t.me/AleXCinemaBot?start=webapp
TELEGRAM_INIT_DATA_MAX_AGE_SECONDS=3600
TELEGRAM_WEB_APP_URL=https://cinax.live/tg-app
TELEGRAM_HOME_URL=https://cinax.live/home

# Dedicated HMAC secret for short-lived Socket.io room tokens (32+ random characters)
SOCKET_AUTH_SECRET=replace_with_a_separate_long_random_server_secret
