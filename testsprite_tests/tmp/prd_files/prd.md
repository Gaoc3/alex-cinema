# Arabseed Premium Backend PRD
This project proxies video streaming requests to cdn.shabakaty.com. It uses Next.js API routes.
The API `/api/stream` fetches video files and streams them back to the client.
The API `/api/proxy` fetches poster images and proxies them to the client.
